const {
  PLANS,
  SUBSCRIPTION_PLANS,
  getPlanConfig,
  getAllPlans,
  getCommissionForPlan,
} = require('../../server/config/plans');

module.exports = {
  PLANS,
  SUBSCRIPTION_PLANS,
  getPlanConfig,
  getAllPlans,
  getCommissionForPlan,
};
